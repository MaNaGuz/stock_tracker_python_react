name: Deploy backend to Cloud Run

on:
  push:
    branches:
      - main
    paths:
      - "py/GCP/**"
      - ".github/workflows/deploy-backend.yml"
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

env:
  PROJECT_ID: stock-tracker-491901
  IMAGE_NAME: backend

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v3
        with:
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.WIF_SERVICE_ACCOUNT }}

      - name: Set up gcloud
        uses: google-github-actions/setup-gcloud@v3

      - name: Build and push image
        run: |
          gcloud builds submit ./py/GCP/ \
            --project="${PROJECT_ID}" \
            --tag "${{ vars.GCP_REGION }}-docker.pkg.dev/${PROJECT_ID}/${{ vars.ARTIFACT_REPO }}/${IMAGE_NAME}:${GITHUB_SHA}"

      - name: Deploy backend
        id: deploy
        uses: google-github-actions/deploy-cloudrun@v3
        with:
          service: ${{ vars.CLOUD_RUN_SERVICE }}
          region: ${{ vars.GCP_REGION }}
          image: ${{ vars.GCP_REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ vars.ARTIFACT_REPO }}/${{ env.IMAGE_NAME }}:${{ github.sha }}

      - name: Output URL
        run: echo "${{ steps.deploy.outputs.url }}"
